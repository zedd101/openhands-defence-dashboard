# AGENTS.md - 작업 및 개발 지침

본 문서는 `04-dashboard` 저장소에서 작업하는 모든 AI 에이전트와 개발자가 반드시 준수해야 하는 최상위 작업 규칙입니다. 세션을 시작하거나 코드를 작성/수정하기 전 반드시 이 규칙을 숙지하고 따라야 합니다.

---

## 1. 프로젝트 개요
* **프로젝트명:** 방산 구매·납기 통합 관리 대시보드
* **목적:** 협력사 조달 부품의 지연이 어떤 군납 계약 일정을 위협하는지 직관적으로 연결하여 모니터링하고, 즉각적인 조치 및 주간 보고 작성을 돕는 단일 페이지 대시보드
* **핵심 타깃:** 코딩 경험이 없는 현업 팀장 및 실무자 (더블 클릭만으로 브라우저에서 즉시 실행)

---

## 2. 필수 절대 규칙 (Core Commandments)

### 📌 데이터 준수 규칙
1. **가상 데이터 독점 사용:** 데이터는 `docs/data.js`에 정의된 가상 데이터만 사용한다.
2. **환각 및 임의 생성 금지:** 데이터에 존재하지 않는 필드나 값을 임의로 지어내지 않는다.
3. **실데이터 전환 원칙:** 단, 추후 정식 API 등을 통해 실제 데이터를 연동하는 기능이 요청될 경우, 기존 가상 데이터는 더 이상 사용하지 않고 실데이터로 완전히 전환한다.
4. **원본 보존:** `docs/data.js` 파일은 임의로 수정하거나 삭제하지 않는다.

### 📌 기술 및 구동 환경 제약
1. **로컬 파일 즉시 실행 보장:** `index.html`은 사용자가 탐색기에서 **더블 클릭(`file:///` 프로토콜)하여 바로 열 수 있어야 한다.**
2. **`fetch()` 절대 사용 금지:** 로컬 환경에서 CORS 보안 에러가 발생하는 `fetch()`, `XMLHttpRequest` 등 비동기 파일 호출 방식은 일체 사용하지 않는다.
3. **스크립트 태그 로드:** `index.html`은 `data.js`를 반드시 HTML 표준 방식인 `<script src="data.js"></script>` 태그로 직접 로드하여 전역 변수를 활용한다.
4. **외부 라이브러리 및 외부 주소 사용 제약:**
   - 외부 라이브러리는 쓰지 않는다
   - 인터넷(외부 주소)은 수집 프로그램(fetch_ 로 시작하는 파이썬 파일)에서만 부른다. index.html 은 저장된 파일만 읽는다

### 📌 인코딩 및 문자셋 규칙
1. **UTF-8 저장 필수:** 모든 파일(HTML, CSS, JS, Markdown 등)은 반드시 **BOM 없는 UTF-8** 형식으로 저장한다.
2. **메타 태그 선언:** `index.html` 최상단 `<head>` 안에 반드시 `<meta charset="UTF-8">`을 명시하여 한글 깨짐을 원천 방지한다.

### 📌 작업 및 코드 수정 범위
1. **최소 변경 원칙:** 기존에 정상 동작하는 기능과 스타일은 온전히 보존하며, 사용자가 요청한 범위만 정밀하게 수정한다.
2. **점진적 개발:** 한 번에 무리하게 코드를 쏟아내지 않고, PRD에 정의된 구현 설계도(1단계~4단계)에 따라 단계별로 검증하며 진행한다.

### 📌 UI/UX 및 디자인 규칙
1. **어조 및 문장:** 화면에 표시되는 모든 사용자 안내 문구는 정중하고 명확한 **존댓말(하십시오체/해요체)**로 작성한다.
2. **가상 데이터 경고 고정:** 화면 최하단(푸터 영역)에 다음 문구를 상시 고정 표시한다:  
   `※ 본 대시보드의 데이터는 교육용 가상 데이터입니다.`
3. **컬러 팔레트 절제:**
   * 배경: 깨끗한 흰색 (`#FFFFFF`) 및 옅은 미색 카드 배경
   * 포인트 컬러: 오렌지 (`#F37321`) 단 1종류만 사용하여 시각적 피로를 방지하고 중요한 행동(버튼, 배지)에만 집중 적용한다.

---

## 3. 디렉토리 구조 및 역할

```
C:\workspace\04-dashboard\
├── .git/               # 형상 관리 저장소
├── docs/               # 브라우저 실행 및 배포 디렉토리
│   ├── data.js         # 방산 가상 데이터셋 (수정 금지)
│   └── index.html      # 단일 대시보드 웹 애플리케이션
├── AGENTS.md           # 본 작업 지침 문서
└── PRD.md              # 요구사항 정의서 및 구현순서 설계도
```

---

## 4. 비즈니스 로직 요약 (참조: PRD.md)

* **기준일자 (`TODAY`):** `2026-09-09`
* **수주잔고 계산:** `CONTRACTS` 중 `stage`가 `"계약"`, `"생산"`, `"납품"`인 사업의 `amount` 합산 (기본 4,501억원)
* **90일 내 납품:** 기준일로부터 90일 이내 계약 건수 집계
* **🔴 적신호 [납기 위협]:** 미입고 발주(`status !== "입고완료"`)의 입고예정일(`eta`)이 해당 계약의 예상납기(`plan`) 7일 전보다 늦은 경우 (`eta > plan - 7일`)
* **🟡 황신호 [지연 / 리스크]:**
  * 입고 지연: `eta > due` 또는 `status === "지연"`
  * 납기 지연 예상: `plan > delivery`
  * 허가 리스크: `expires <= 30일` 또는 생산·납품 단계에서 `status !== "승인"`
* **주간 보고 요약 복사:** 현재 집계된 수치를 바탕으로 5줄 텍스트 요약문을 생성하여 원클릭 클립보드 복사 제공
